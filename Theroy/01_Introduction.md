# How computers work
This is the basic operations on how the computer work from root up.
On the root level the computer is made up of transistors which is made up of silicion and electrodes.
The silicon allow the current to pass throught depending on the current level and when the current pass it can b deduced as 1 and other wise it will b 0 so this is the start of binary

## Logic gates
These are the foundation of the calculation based on the input from the slicon we can perform the operations on the current using these gates they include
- Not : The not gate reverses the input 0 ---> 1 and vice versa
- OR : It will return 1 if both of the inputs are one or either one of the input is one
- AND : It will return 1 only if both of the inputs are one otherwise its 0
- XOR : It will return 0 if both inputs are one or 0 otherwise if one of the input is 1 it will return 1

## How computers do arithematic
In computers the arithematic works by using the binary instead of using the base 10 like normally we convert the based 10 numbers into base 2 numbers and then perform actions on them and we can do the arithmatic on the number using the logic gates like can perform binary addition using the xor gate.

## Computer memory
Computer stores data using SR latch we do is that it has two wires we set S wire to our disered save result we save the input like we can save each binary number like 0 1 and all the possible combinations of these using multipel of these latches to rember the state